{% extends 'WebAdminBundle::base.html.twig' %}

{% block content_wrapper %}
    <h1>Zone creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <br/>
        <input class="btn btn-primary" type="submit" value="Create" />
    {{ form_end(form) }}

   <br/>
            <a class="btn btn-primary" href="{{ path('Admin_Zone_index') }}">Back to the list</a>
       
{% endblock %}
