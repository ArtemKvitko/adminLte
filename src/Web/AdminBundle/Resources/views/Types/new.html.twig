{% extends 'WebAdminBundle::base.html.twig' %}

{% block content_wrapper %}
    <h1>Types creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <br/>
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <br/>
            <a class="btn btn-primary" href="{{ path('Admin_Types_index') }}">Back to the list</a>
        
{% endblock %}
