{% extends 'WebAdminBundle::base.html.twig' %}

{% block content_wrapper %}
    <h1>Amortization creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('amortization_index') }}" class="btn btn-primary">Back to the list</a>
        </li>
    </ul>
{% endblock %}
